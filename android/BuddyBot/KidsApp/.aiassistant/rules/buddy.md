---
apply: always
---

1. Environment & ToolchainJava Standard: Strictly use JDK 17. If the code uses legacy Java 8/11 syntax or libraries, rewrite it for Java 17 compatibility.Build System: Target Android Gradle Plugin (AGP) 8.0+ and Gradle 8.7.Dependencies: Use androidx.media3 for all video/audio (ExoPlayer). Do not use the legacy com.google.android.exoplayer2.2. The "Face" State Machine (S9)Transition Logic: Every mode change must follow this sequence: [current_mode]_to_normal.mp4 (Play Once) $\rightarrow$ normal_idle.mp4 (Loop).Lip-Sync Logic: Toggle between [mode]_idle.mp4 and [mode]_talk.mp4 based on the isSpeaking boolean.Interrupts: Any US:F < 20 (Front Sensor) data must immediately pause the current loop and play bodyguard_warning.mp4.3. Security & Handshake (S9 ↔ R4)Protocol: Never change modes on the S9 without a validated PIN.Flow: R4 sends REQ_MODE:[name] $\rightarrow$ S9 triggers PasscodeDialog $\rightarrow$ PIN entered on S9 (or R4 KEY:[n] events) $\rightarrow$ If valid, S9 sends UNLOCK_OK to Mega/R4 $\rightarrow$ Face transitions.4. Sensor FusionMapping: Parse only the 4-way fixed array: US:F (Front), US:R (Rear), US:L (Left), US:Ri (Right).Hardware: No radar logic. Only 4-zone proximity triggers.